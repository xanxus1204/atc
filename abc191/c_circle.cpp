#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    double X,Y,R; cin >> X >> Y >> R;
    vector<vector<ll>> (2*R, ) ;
    if (X > 0 && Y > 0) {
        cout << "Yes" << endl;
    }
    return 0;
}