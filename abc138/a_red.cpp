#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    int a;
    string s;
    cin >> a >> s;
    if (a >= 3200){
        cout << s;
    }else{
        cout << "red";
    }
    return 0;
}