#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    ll N; cin >> N;
    ll limit = 1;
    int ans = 0;
    for (while limit <= N) {
        int tmp = int()
    }
    return 0;
}