#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    while(true){
        for alpha in "ABCDEFGHIJKLMNOPQRSTUVWXYZ":REP
    }
    return 0;
}