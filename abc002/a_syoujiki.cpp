#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    ll X,Y;
    cin >> X >> Y;
    if (X > Y) {
        cout << X << endl;
    }else{
        cout << Y << endl;
    }
    return 0;
}