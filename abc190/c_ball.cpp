#include<bits/stdc++.h>
#define REP(i,n) for (int i=0;i<(n);i++)
using namespace std;
typedef long long ll;
int main() {
    int N, M;
    cin >> N >> M;
    vector<int> A(M), B(M);
    REP(i,M) cin >> A[i] >> B[i];
    int K;
    cin >> K;
    vector<int> C(K), D(K);
    REP(i, K) cin >> C[i] >> D[i];


    return 0;
}